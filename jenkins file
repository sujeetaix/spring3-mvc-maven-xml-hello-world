pipeline {
    agent any

    stages {
        stage('git scm') {
            steps {
             checkout([$class: 'GitSCM', branches: [[name: '*/master']], url: 'https://github.com/Alimelu/spring3-mvc-maven-xml-hello-world.git']]]) 
            }
        }
        
    }
}
